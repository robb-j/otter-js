version: '2'

volumes:
  database_data:
    driver: local


services:
  web:
    build: .
    links:
      - mongo
    environment:
      MONGO_URL: 'mysql'
    command: node node_modules/.bin/nodemon --debug=0.0.0.0:5858 --watch web web/server.js

  mongo:
    image: mongo:latest
    container_name: 'ottter-mongo'
    ports:
      - 27017:27017
    volumes:
      - database_data:/data
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: secret
